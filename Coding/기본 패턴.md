# 1) **입력 구조(Input Handling)**

코테에서 입력 처리는 생명이라서 구조를 반드시 알아야 해.

### 기본 구조
`import sys input = sys.stdin.readline  n = int(input()) arr = list(map(int, input().split()))`

### 여러 줄 반복 입력

`for _ in range(n):     a, b = map(int, input().split())`

### 문자열 입력

`s = input().rstrip()  # 개행 제거`

👉 이 정도면 거의 모든 입력 구조 해결됨.

---

# 2) **리스트 구조(List Structure)**

파이썬에서 리스트는 **동적 배열(Dynamic Array)** 구조야.

- append: 평균 O(1)
    
- pop(): 뒤에서 O(1)
    
- pop(0): O(n) → 절대 쓰지 말기
    
- sort(): O(n log n)
    

### 필수 패턴

`arr = [3, 1, 4] arr.append(2) arr.sort() arr.sort(reverse=True)`

### 2차원 리스트(격자)

`board = [list(map(int, input().split())) for _ in range(n)]`

👉 2차원 리스트는 BFS/DFS 문제에서 필수.

---

# 3) **딕셔너리 구조(Dictionary / HashMap)**

파이썬 딕셔너리는 **해시 테이블 구조**로 되어 있어  
조회·추가·삭제가 평균 O(1).

### 기본

`d = {} d[key] = value`

### 존재 확인

`if key in d:     ...`

### 빈도 세기

`from collections import Counter cnt = Counter(arr)`

👉 문자열 문제, 빈도 문제에서 최강.

---

# 4) **집합 구조(Set Structure)**

파이썬 set은 **해시 기반 집합**.

- 중복 제거
    
- 빠른 포함 여부 확인 (`in`이 평균 O(1))
    

### 구조

`s = set() s.add(x)`

### 리스트 → 중복 제거

`unique = set(arr)`

👉 “이미 방문한 값인지 확인해야 하는 문제”에서 많이 씀.

---

# 5) **큐/스택 구조(Queue / Stack)**

파이썬 리스트는 O(n) 문제 때문에 스택 전용으로만 쓰고  
큐는 무조건 deque 사용.

### 스택

`stack = [] stack.append(x) stack.pop()`

### 큐

`from collections import deque q = deque() q.append(x)  # push q.popleft()  # pop`

👉 BFS, 시뮬레이션, 토폴로지 정렬에서 필수.

---

# 6) **우선순위 큐 구조(Heap Structure)**

파이썬은 **최소 힙(min-heap)**만 제공함.

### 기본 구조

`import heapq  hq = [] heapq.heappush(hq, value) min_value = heapq.heappop(hq)`

### 최대 힙 만들기

`heapq.heappush(hq, -value) value = -heapq.heappop(hq)`

👉 다익스트라, K번째 큰 수, 정렬 대체 문제에서 필요.

---

# 7) **그래프 구조(Graph Structure)**

코테에서 가장 자주 만나는 구조.

### 1) 인접 리스트 구조

노드가 많을 때 효율적.

`graph = [[] for _ in range(n+1)] for _ in range(m):     a, b = map(int, input().split())     graph[a].append(b)     graph[b].append(a)   # 무방향`

### 2) BFS 구조

`from collections import deque  def bfs(start):     q = deque([start])     visited[start] = True     while q:         x = q.popleft()         for nx in graph[x]:             if not visited[nx]:                 visited[nx] = True                 q.append(nx)`

### 3) DFS 구조

`def dfs(x):     visited[x] = True     for nx in graph[x]:         if not visited[nx]:             dfs(nx)`

👉 BFS/DFS는 암기해야 할 “구조” 그 자체.

---

# 🔥 추가: 코테에서 자주 쓰는 구조적 패턴

## ✔ 정렬 구조

`arr.sort(key=lambda x: (x[0], -x[1]))`

## ✔ 누적합 구조

`prefix = [0] * (n+1) for i in range(n):     prefix[i+1] = prefix[i] + arr[i]  # 구간합 result = prefix[r] - prefix[l-1]`

## ✔ 투 포인터 구조

`left, right = 0, n-1 while left < right:     s = arr[left] + arr[right]`

## ✔ 슬라이딩 윈도우

`window_sum = sum(arr[:k]) for i in range(k, n):     window_sum += arr[i] - arr[i-k]`